# Minimal requirements for memory-constrained deployment (Railway, etc.)
# This file excludes heavy ML dependencies for deployments with <1GB RAM

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn>=21.2.0

# Database
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.0  # PostgreSQL
pymysql>=1.1.0  # MySQL

# Data validation
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv==1.0.0

# HTTP client
httpx==0.25.2
python-multipart==0.0.6

# Authentication
PyJWT>=2.8.0
bcrypt>=4.0.0
cryptography>=41.0.0

# Rate limiting
slowapi>=0.1.9

# Cloud LLM providers (API-based, no heavy models)
openai>=1.0.0  # OpenAI API
anthropic>=0.7.0  # Anthropic Claude API

# Supabase client
supabase>=2.0.0

# Lightweight utilities
numpy>=1.24.0,<2.0.0

# Document processing (lightweight)
PyPDF2>=3.0.0
python-docx>=1.0.0

# Note: The following heavy dependencies are EXCLUDED for minimal deployment:
# - sentence-transformers (80-150MB + model downloads)
# - chromadb (vector database overhead)
# - transformers (HuggingFace, 500MB+)
# - accelerate
# - ollama
# - nltk
# - pandas
# - pypdf
# - huggingface_hub
#
# If you need RAG/embeddings, use API-based services like:
# - OpenAI embeddings API
# - Cohere embeddings API
# - Voyage AI embeddings API
